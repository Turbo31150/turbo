# TASK 20260305_001 - Diagnostic et correction post-audit JARVIS

## Priorite: HIGH
## Assigne a: perplexity_jarvis3
## Date: 2026-03-05

## Contexte
Audit complet JARVIS realise. 6 alertes detectees par Claude Code.

## Actions a executer (dans l'ordre)

### 1. Diagnostic M3 (192.168.1.113)
Verifier si la machine M3 est accessible sur le reseau :
```powershell
Test-Connection -ComputerName 192.168.1.113 -Count 2 -Quiet
```
Si accessible, verifier si LM Studio tourne :
```powershell
Invoke-RestMethod -Uri "http://192.168.1.113:1234/api/v1/models" -TimeoutSec 5
```

### 2. Demarrer les 3 services manquants
Dashboard (port 8080) :
```powershell
Start-Process -FilePath "cmd" -ArgumentList "/c cd F:\BUREAU\turbo\dashboard && npm start" -WindowStyle Hidden
```
Python WS (port 9742) :
```powershell
Start-Process -FilePath "cmd" -ArgumentList "/c cd F:\BUREAU\turbo\python_ws && uv run python main.py" -WindowStyle Hidden
```
Canvas proxy (port 18800) :
```powershell
Start-Process -FilePath "cmd" -ArgumentList "/c cd F:\BUREAU\turbo\canvas && node direct-proxy.js" -WindowStyle Hidden
```

### 3. Verifier finetuning.db
```python
import sqlite3
conn = sqlite3.connect("F:/BUREAU/turbo/finetuning/finetuning.db")
cur = conn.cursor()
cur.execute("SELECT name FROM sqlite_master WHERE type='table'")
tables = cur.fetchall()
print(f"Tables: {tables}")
# Si vide, signaler pour recreation
conn.close()
```

### 4. Surveiller GPU0 temperature
```powershell
nvidia-smi --query-gpu=index,temperature.gpu,power.draw --format=csv,noheader -i 0
```
Si > 80C, reduire la charge M1.

## Resultat attendu
- M3 status (online/offline + raison)
- 3 services demarres (ports 8080, 9742, 18800 confirmes)
- finetuning.db status (tables presentes ou a recreer)
- GPU0 temperature actuelle

## Report back
Ecrire le resultat dans: F:\BUREAU\turbo\tasks\from_perplexity\result_20260305_001.md

=== READY FOR PERPLEXITY ===
